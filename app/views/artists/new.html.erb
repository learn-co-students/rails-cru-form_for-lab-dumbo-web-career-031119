<h1>New Artist Form</h1>
<%= form_for @artist do |f| %>
<label>Artist name: </label>
<%= f.text_field :name %>
<%= f.label :bio %>
<%= f.text_field :bio %>

<%= f.submit "Create Artist" %>

<% end %>
