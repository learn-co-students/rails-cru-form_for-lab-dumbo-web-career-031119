<%= form_for @artist do |f|%>
<%= label_tag 'artist[name]','Artist name'%>
<%= text_field_tag 'artist[name]',"#{@artist.name}"%>
<%= label_tag 'artist[bio]','Artist bio'%>
<%= text_area_tag 'artist[bio]',"#{@artist.bio}"%>
<%= submit_tag 'Update Artist'%>
<%end%>
